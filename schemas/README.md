Shared schemas live here:

- manifest.json schema (runner bundle file listing + hashes)
- index.json schema (local registry index)
- params.json and dataset.json are per-runner under runner folders


